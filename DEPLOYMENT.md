# Deployment

> Placeholder.

This document will cover deploying Kairos: building Soroban contracts to testnet/mainnet, running the backend via `backend/Dockerfile` and `docker-compose.yml`, deploying the dashboard to Vercel (`apps/web` + root `vercel.json`), and required environment variables (see [`README.md`](./README.md#environment-variables)).
