# SDK

> Placeholder. Authoritative SDK documentation lives in [`packages/sdk/README.md`](../packages/sdk/README.md) and [`docs/api/API.md`](../docs/api/API.md).

This document will cover the `@wolf1276/kairos-sdk` modules: `wallet`, `delegation`, `policy`, `execution`, `events`, and `registry`, plus the typical request flow for building, signing, and redeeming delegations.
