# API Reference

> Placeholder. The backend REST API is documented in [`backend/README.md`](../backend/README.md); the SDK API in [`docs/api/API.md`](../docs/api/API.md).

This document will consolidate the backend REST API (auth, agents, strategies, positions, trades, audit, smart-wallets, context, dashboard, benchmark, dev) and the frontend GraphQL price API.
